const initialState = {

}

const InvoicingAndPaymentReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return { state }
    }
}

export default InvoicingAndPaymentReducer;
